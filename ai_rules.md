# AI AGENT PRIME DIRECTIVES
1. NO UNAUTHORIZED CHANGES: You must explicitly ask the user for permission before modifying, deleting, or refactoring any core routing, API, or engine logic.
2. EXACT SCOPE ONLY: Only modify the exact lines requested. Do not attempt to "clean up" or "improve" unrelated code in the same file.
3. MANDATORY CHANGELOG: Every time you modify a file, you must append a brief summary of the exact changes made to a file named CHANGELOG.md.
4. API CAUTION: Never change API endpoints, base URLs, or model names unless explicitly instructed by the user.